% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ep2b.R
\name{ep2b}
\alias{ep2b}
\title{Read and peek at all sheets in an Excel workbook}
\usage{
ep2b(
  path,
  sheets = NULL,
  skip_empty = TRUE,
  show = TRUE,
  envir = parent.frame()
)
}
\arguments{
\item{path}{Path to an Excel file.}

\item{sheets}{Optional character vector of sheet names to read. If \code{NULL}
(the default), all sheets are read.}

\item{skip_empty}{Logical. If \code{TRUE} (default), sheets with zero rows are
dropped from the result.}

\item{show}{Logical. If \code{TRUE} (default), print a summary report when the
function returns.}

\item{envir}{Environment into which the individual sheet tibbles are
assigned. Defaults to the caller's environment (i.e., the global
environment when called interactively).}
}
\value{
Invisibly returns an object of class \code{workbook_peek}: a named list
of tibbles (one per sheet) with a custom print method. Also assigns
each sheet as an individual tibble in \code{envir} (as a side effect).
}
\description{
Reads every sheet from an \code{.xlsx} or \code{.xls} file into individual tibbles
in the calling environment, prints a summary of each sheet's shape and
column-level information, and invisibly returns the sheets as a named
list.
}
\examples{
example_file <- system.file("extdata", "example.xlsx", package = "ep2b")
ep2b(example_file)

}
